version: "3"

services:
 app:
    build: .
    image: lifelog
    ports:
      - 8080:8080
    env_file:
      - app.env
    depends_on:
      - db
 db:
    image: postgres
    env_file:
      - db.env
